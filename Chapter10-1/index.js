/**
 * 项目内配置
 */
const { config } = require('sugar-electron');
console.log('config', config);

/**
 * 开启全局配置
 */
// const { config } = require('sugar-electron');
// config.setOption({ global: true });

// console.log('config', config);
